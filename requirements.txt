# Core dependencies
neo4j>=5.0
spacy>=3.5.0

# Embedding providers (choose one during setup):
#   pip install agent-brain[local]     → sentence-transformers (local, default)
#   pip install agent-brain[openai]    → OpenAI API
#   pip install agent-brain[ollama]    → Ollama (local API, requires httpx)
